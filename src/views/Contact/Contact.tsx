import React from "react";
import styled from "@emotion/styled";
import { useParams } from "react-router-dom";
import { ContactFirstSection } from "../../sections/ContactFirstSection";






interface HomeProps {}
export const Contact = (props: HomeProps) => {
  return (
    <div>
    <ContactFirstSection/>

    </div>
  );
};